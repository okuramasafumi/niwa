# Niwa document

## Body
<% registry.each do |item| %>
  ### <%= "#{item.klass}##{item.name}" %>

  - Generated by: <%= item.plugin_name %>
  - Defined in: <%= item.filename %>
  - Metadata:
  <% item.metadata.each do |k, v| %>
    - <%= "#{k}: #{v}" %>
  <% end %>
<% end %>
